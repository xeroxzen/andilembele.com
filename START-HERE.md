Clone https://github.com/Thabhelo/andile, then read the website, README.md, HANDOFF.md, and the application code. This is my personal website: I am Andile Jaden Mbele. Help me finish configuring everything needed to run the website, writing section, and CMS on my own accounts, and connect my existing domain, andilembele.com.

Start by running and inspecting the site locally. Review the public pages, responsive layout, Writing page, and local editor. Preserve the current design, experience timeline, projects, and factual content. Ask me about any uncertain details rather than inventing them. Explain the current state and propose the simplest maintainable production setup, including expected recurring costs.

Guide me through setup step by step. When an account login, verification, payment, or owner decision is needed, pause and tell me exactly what to do. Let me enter passwords, MFA codes, payment details, and secrets myself through the appropriate service. Never ask me to paste secrets into chat or commit them. Use only accounts and infrastructure I own or explicitly authorize. Ask before creating paid resources or changing production services.

Repository ownership: the source repository is Thabhelo/andile. Ask whether I want my own fork, a new repository, or an owner-approved transfer. Treat the source as read-only unless its owner explicitly authorizes changes. Confirm the intended repository and authenticated GitHub identity immediately before every GitHub write. Do not use someone else's stored credentials.

Hosting and backend: help me choose hosting, persistent database, media storage, and administrator authentication under my own accounts. Inspect the existing implementation before choosing an architecture. The production build already uses Google OAuth and Firestore with two allowlisted accounts. Read FIREBASE-SETUP.md and BACKEND-AUDIT.md first, verify the existing integration, and preserve its rules. The separate local server must remain loopback-only. Keep drafts private, validate uploads, store secrets server-side, and configure suitable security rules, request limits, and backups. Preserve Markdown editing, preview, tags, dates, excerpts, slugs, cover/inline images, import/export, and unsaved-work recovery. Migrate existing local posts and media, if any, without losing them. Demonstrate a backup and restore.

Medium: my existing writing is at https://medium.com/@andilembele. The website includes a metadata snapshot linking to original articles and a Python refresh script exposed through npm run sync:medium. Inspect and verify these links. Keep Medium articles alongside native published posts in the searchable Writing page, with clear source labels and dates. Configure a reliable scheduled or build-time RSS refresh and refresh homepage highlights too. Preserve older indexed articles and the last good snapshot if Medium is unavailable. Public RSS needs no Medium login. Ask before copying full articles; if I choose to republish, arrange original-source links and canonical URLs to avoid competing duplicates. Do not invent essays or publication dates.

Domain: I already have access to andilembele.com. Ask me to sign in to my existing registrar/DNS provider and chosen hosting service. Inspect the current DNS and existing site routes, record the current website records for rollback, and show me the exact changes needed. Confirm my preferred primary hostname, apex or www. Preserve mail records including MX, SPF, DKIM and DMARC, plus unrelated subdomains and verification records. Do not replace nameservers or remove records without explaining the consequences and getting approval. Connect the domain using the hosting provider's verified instructions. Set up HTTPS, a redirect from the alternate hostname, redirects for important old URLs, canonical metadata, social previews, sitemap and RSS. Keep private drafts and admin pages out of indexing; robots directives do not replace authentication.

Contact and optional services: verify my existing contact links. Keep email links unless I request a contact form. Ask before adding analytics, newsletters, comments, or mail services; use my accounts and explain any costs and privacy implications.

Validation: run the existing checks and add meaningful tests for the production changes. Verify login/logout, blocked unauthorized writes, draft privacy, publish/unpublish, editing, uploads, media persistence after restart/deploy, safe Markdown, backups, article routes, search and topics, Medium links and refresh failures, keyboard/mobile navigation, and broken links. Create a staging preview after I approve the provider and resources. Show me the working site, remaining issues, exact DNS changes, costs, and rollback plan. Obtain my explicit approval before production deployment or DNS cutover.

After approval, complete deployment and domain connection, verify HTTPS and redirects on the real domain, and check that domain email settings were preserved. Give me a short owner guide covering how to log in, write and publish, refresh Medium links, manage media, restore backups, renew the domain, and find hosting/billing settings. List the services and accounts involved without revealing secret values. Distinguish completed work, verified results, and anything still requiring my action. Continue until the approved setup works or a specific action from me is required.

Admin entry point: inspect `/admin` and `/admin/posts`. The production `/admin` uses Google sign-in; test it with andilembele020@gmail.com. Keep privileged operations protected by Firestore rules. Preserve the existing dashboard, post editing, draft/visible states, deletion confirmation, media, previews, import/export, and recovery.

Firebase has now been initialized in the dedicated personal-account project `andile-portfolio-personal`. Read FIREBASE-SETUP.md and BACKEND-AUDIT.md before provisioning. Do not create another project by default. Complete authorized ownership handover and verify the existing Google OAuth and Firestore integration. Billing is currently disabled.
